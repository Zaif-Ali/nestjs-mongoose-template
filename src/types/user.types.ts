// User Roles
export enum UserRole {
  admin = 'admin',
  vendor = 'vendor',
  consumer = 'consumer',
}

// User Genders
export enum UserGender {
  male = 'male',
  female = 'female',
  other = 'other',
}
